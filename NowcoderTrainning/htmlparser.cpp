//
// Created by devinchang on 2019/9/10.
//
#include <iostream>
#include <string>
#include <vector>
#include <stack>
#include <algorithm>
using namespace std;

bool isValid(string &str){
    if(str.size() < 7 || str[0] != '<' || str.back() != '>')
        return false;
    stack<string> name;
}


int main(){
    ios::sync_with_stdio(false);
    string str;
    cin >> str;

    return 0;
}

